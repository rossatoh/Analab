// Service disabled due to removal of AI features
export const generateVintageSlogan = async (topic: string): Promise<string> => {
  return "Funcionalidade desativada.";
};

export const generateBlogIdeas = async (): Promise<string[]> => {
  return [];
};